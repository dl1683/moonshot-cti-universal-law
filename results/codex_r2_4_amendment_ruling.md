The first two amendments match the defect evidence: the invalid branch currently flips harm exactly backward and omits the protocol-required amount NME=10; the sealed scorer receives only integer `gold_array` values. The remaining architectural question is whether Change 3 can honestly satisfy the frozen “outer watchdog” contract, and whether the prior runner spec needs revision-number and stop-state updates.